<!DOCTYPE html>

<title>Bienvenue !</title>

{% block body %}
    <style>
        .accueil-wrapper { background: #f3dac5; margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .accueil-wrapper code { background: #efd2b7; padding: 2px 6px; }
    </style>

    <div class="accueil-wrapper">
        <h1>Bonjour {{ user.login }}! ✅</h1>

        <p>
            {{ welcome }} {{ nameSite }}
        </p>
        <p>
            Vous êtes connecté en tant
            {%  if user is null %}
                qu'invité
            {% elseif not user.getIsAdmin() %}
                que membre
            {% elseif user.getIsSuperAdmin() %}
                que super-administrateur
            {% else %}
                qu'administrateur
            {% endif %}
        </p>
    </div>
{% endblock %}